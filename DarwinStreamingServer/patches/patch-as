$NetBSD: patch-as,v 1.1.1.1 2005/12/20 19:03:10 danieleggert Exp $
--- Buildit.orig	2005-05-31 12:54:23.000000000 +0200
+++ Buildit	2005-12-17 22:03:30.000000000 +0100
@@ -109,6 +109,18 @@
 		MODULE_LIBS="-lgcc -lstdc++"
 		;;
 	
+    NetBSD.*)
+    echo "Configuring for the "$OSNAME" "$HARDWARENAME" platform"
+    CPLUS=gcc
+    CCOMP=gcc
+    LINKER="gcc"
+    MAKE=make
+    COMPILER_FLAGS=
+    INCLUDE_FLAG="-include"
+    CORE_LINK_LIBS="-pthread -lm -lcrypt -lstdc++"
+    SHARED=-shared
+    MODULE_LIBS="-lgcc -lstdc++"
+    ;;
 #	SunOS.sun4m | \
 #	SunOS.sun4u)
 	SunOS.*)
@@ -205,6 +217,14 @@
 		;;
 esac
 
+if [ ! -z "${PKG_PHASE}" ] ; then
+    echo "Overriding configuration for pkgsrc"
+    CPLUS=gcc
+    CCOMP=gcc
+    LINKER=gcc
+    MAKE=${PKGSRCMAKE}
+fi
+
 if [ "$*" = "install" ] ; then
 
         if [ `uname` != "SunOS" ]; then
