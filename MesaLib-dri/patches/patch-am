$NetBSD: patch-am,v 1.4 2007/07/24 19:30:12 bsadewitz Exp $

--- src/mesa/shader/program.c.orig	2007-06-21 18:10:54.000000000 -0400
+++ src/mesa/shader/program.c
@@ -333,7 +333,7 @@ _mesa_clone_program(GLcontext *ctx, cons
 {
    struct gl_program *clone;
 
-   clone = _mesa_new_program(ctx, prog->Target, prog->Id);
+   clone = ctx->Driver.NewProgram(ctx, prog->Target, prog->Id);
    if (!clone)
       return NULL;
 
