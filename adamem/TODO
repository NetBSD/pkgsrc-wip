Installs and runs well for me on i386.  Uses optional in-line i386 asm
code, and requires ENDIAN-ness to be defined without a configure file, so: 

1. Should  be tested to make sure conditionals in Makefile produce good
working binary packages on non-i386 and BIG_ENDIAN architectures...
