$NetBSD: patch-docs_man_man1_ansible-galaxy.1,v 1.0 2016/02/24 22:43:35 tty Exp $

Fix hardcoded paths.

--- docs/man/man1/ansible-galaxy.1.orig	2017-09-19 02:57:30.000000000 +0000
+++ docs/man/man1/ansible-galaxy.1
@@ -84,7 +84,7 @@ Don\(cqt query the galaxy API when creat
 .PP
 \fB\-p\fR, \fB\-\-roles\-path\fR
 .RS 4
-The path to the directory containing your roles\&. The default is the roles_path configured in your ansible\&.cfgfile (/etc/ansible/roles if not configured)
+The path to the directory containing your roles\&. The default is the roles_path configured in your ansible\&.cfgfile (@PKG_SYSCONFDIR@/ansible/roles if not configured)
 .RE
 .RE
 .PP
@@ -94,7 +94,7 @@ searches for roles on the Ansible Galaxy
 .PP
 \fB\-p\fR, \fB\-\-roles\-path\fR
 .RS 4
-The path to the directory containing your roles\&. The default is the roles_path configured in your ansible\&.cfgfile (/etc/ansible/roles if not configured)
+The path to the directory containing your roles\&. The default is the roles_path configured in your ansible\&.cfgfile (@PKG_SYSCONFDIR@/ansible/roles if not configured)
 .RE
 .PP
 \fB\-\-author\fR \fIAUTHOR\fR
@@ -134,7 +134,7 @@ lists the roles installed on the local s
 .PP
 \fB\-p\fR, \fB\-\-roles\-path\fR
 .RS 4
-The path to the directory containing your roles\&. The default is the roles_path configured in your ansible\&.cfgfile (/etc/ansible/roles if not configured)
+The path to the directory containing your roles\&. The default is the roles_path configured in your ansible\&.cfgfile (@PKG_SYSCONFDIR@/ansible/roles if not configured)
 .RE
 .RE
 .PP
@@ -144,7 +144,7 @@ removes the list of roles passed as argu
 .PP
 \fB\-p\fR, \fB\-\-roles\-path\fR
 .RS 4
-The path to the directory containing your roles\&. The default is the roles_path configured in your ansible\&.cfgfile (/etc/ansible/roles if not configured)
+The path to the directory containing your roles\&. The default is the roles_path configured in your ansible\&.cfgfile (@PKG_SYSCONFDIR@/ansible/roles if not configured)
 .RE
 .RE
 .PP
@@ -199,7 +199,7 @@ Ignore errors and continue with the next
 .PP
 \fB\-p\fR, \fB\-\-roles\-path\fR
 .RS 4
-The path to the directory containing your roles\&. The default is the roles_path configured in your ansible\&.cfgfile (/etc/ansible/roles if not configured)
+The path to the directory containing your roles\&. The default is the roles_path configured in your ansible\&.cfgfile (@PKG_SYSCONFDIR@/ansible/roles if not configured)
 .RE
 .PP
 \fB\-r\fR \fIROLE_FILE\fR, \fB\-\-role\-file\fR \fIROLE_FILE\fR
@@ -256,7 +256,7 @@ ANSIBLE_CONFIG \(em Override the default
 Many more are available for most options in ansible\&.cfg
 .SH "FILES"
 .sp
-/etc/ansible/ansible\&.cfg \(em Config file, used if present
+@PKG_SYSCONFDIR@/ansible/ansible\&.cfg \(em Config file, used if present
 .sp
 ~/\&.ansible\&.cfg \(em User config file, overrides the default config if present
 .SH "AUTHOR"
