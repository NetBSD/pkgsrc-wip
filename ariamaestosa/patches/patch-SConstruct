$NetBSD: patch-SConstruct,v 1.1 2012/09/09 17:01:23 othyro Exp $

Fixes build environment PATH variable.

--- SConstruct.orig	2012-09-03 18:56:37.000000000 +0000
+++ SConstruct
@@ -198,7 +198,7 @@ def compile_Aria(which_os):
     
     env.Decider('MD5-timestamp')
     
-    env.Append(PATH = os.environ['PATH'])
+    env['ENV']['PATH'] = os.environ.get('PATH')
 
     if 'CXX' in os.environ:
         print ">> Using compiler " + os.environ['CXX']
