$NetBSD: patch-aa,v 1.1.1.1 2003/06/30 00:40:20 felix9x Exp $

--- boost_1_30_0/boost/config/compiler/gcc.hpp.orig	2003-01-30 18:13:55.000000000 +0000
+++ boost_1_30_0/boost/config/compiler/gcc.hpp
@@ -59,7 +59,7 @@
 #endif
 //
 // last known and checked version is 3.2:
-#if (__GNUC__ > 3) || ((__GNUC__ == 3) && (__GNUC_MINOR__ > 2))
+#if (__GNUC__ > 3) || ((__GNUC__ == 3) && (__GNUC_MINOR__ > 3))
 #  if defined(BOOST_ASSERT_CONFIG)
 #     error "Unknown compiler version - please run the configure tests and report the results"
 #  else
