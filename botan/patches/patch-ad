$NetBSD: patch-ad,v 1.1 2009/09/05 18:46:30 asau Exp $

Call prefix "PREFIX", not "DESTDIR".
Implement DESTDIR installation.

--- src/build-data/makefile/nmake.in.orig	2009-09-05 22:33:03.000000000 +0400
+++ src/build-data/makefile/nmake.in	2009-09-05 22:32:47.000000000 +0400
@@ -17,11 +17,11 @@
 ##################################################
 # Installation Settings                          #
 ##################################################
-DESTDIR       = %{prefix}
+PREFIX       = %{prefix}
 
-LIBDIR        = $(DESTDIR)\%{libdir}
-HEADERDIR     = $(DESTDIR)\%{includedir}\botan
-DOCDIR        = $(DESTDIR)\%{docdir}\Botan-$(VERSION)
+LIBDIR        = $(PREFIX)\%{libdir}
+HEADERDIR     = $(PREFIX)\%{includedir}\botan
+DOCDIR        = $(PREFIX)\%{docdir}\Botan-$(VERSION)
 
 ##################################################
 # Aliases for Common Programs                    #
