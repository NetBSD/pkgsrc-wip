$NetBSD: patch-aa,v 1.1.1.1 2008/12/24 13:20:52 thomasklausner Exp $

--- Makefile.orig	2008-12-15 06:55:43.000000000 +0100
+++ Makefile
@@ -6,7 +6,7 @@ prefix = /usr/local
 ################
 
 ifneq ($(findstring gcc,$(compiler)),) # GCC family
-  flags = -O3 -fomit-frame-pointer $(if $(call streq,$(platform),x),-mtune=native,) -Ilib
+  flags = -O3 -fomit-frame-pointer -Ilib
   c = $(compiler) $(flags)
   cpp = $(subst cc,++,$(compiler)) $(flags)
   obj = o
