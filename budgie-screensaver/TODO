Check gettext bl3.  It was changed from gettext/ to gettext-lib/
without understanding, because gettext/buildlink3.mk does not exist.

Check/fix HOMEPAGE etc; the world has changed and
https://github.com/BuddiesOfBudgie might be the right place.

Fix COMMENT.

Fix DESCR.

Fix PLIST.
