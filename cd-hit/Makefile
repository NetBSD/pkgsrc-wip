# $NetBSD: Makefile,v 1.1 2015/09/18 13:44:08 outpaddling Exp $
#
###########################################################
#               Generated by fbsd2pkg                     #
###########################################################

DISTNAME=	cd-hit-v${PKG_VERSION}-2015-0603
PKGNAME=	cd-hit-${PKG_VERSION}
CATEGORIES=	biology
MASTER_SITES=	${MASTER_SITE_GITHUB:=weizhongli/cdhit/releases/download/V${PKG_VERSION}/}

MAINTAINER=	jwbacon@tds.net
HOMEPAGE=	http://weizhong-lab.ucsd.edu/cd-hit/
COMMENT=	Clustering and comparing protein or nucleotide sequences
LICENSE=	gnu-gpl-v2

USE_LANGUAGES=	c++
USE_TOOLS+=	bash:run gmake
REPLACE_PERL=	*.pl

SUBST_CLASSES+=		compiler
SUBST_STAGE.compiler=	post-patch
SUBST_FILES.compiler=	${WRKSRC}/${MAKE_FILE}
SUBST_SED.compiler=	-e 's|g++|${CXX:Q}|g'

SUBST_CLASSES+=		destdir
SUBST_STAGE.destdir=	post-patch
SUBST_FILES.destdir=	${WRKSRC}/${MAKE_FILE}
SUBST_SED.destdir=	-e 's|$$(PREFIX)|$${DESTDIR}$${PREFIX}/bin|g'

SUBST_CLASSES+=		bash
SUBST_STAGE.bash=	post-patch
SUBST_FILES.bash=	${WRKSRC}/cd-hit-2d-para.pl ${WRKSRC}/cd-hit-para.pl
SUBST_SED.bash=		-e 's|/bin/bash|${PREFIX}/bin/bash|g'

MAKE_FILE=	makefile
MAKE_FLAGS+=	openmp=yes

INSTALLATION_DIRS=	bin

# Fails on NetBSD
FETCH_USING=	curl

PKG_VERSION=	4.6.4

.include "../../mk/bsd.pkg.mk"
