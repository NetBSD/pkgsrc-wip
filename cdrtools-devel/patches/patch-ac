$NetBSD: patch-ac,v 1.3 2006/09/19 20:06:13 thomasklausner Exp $

--- include/schily/btorder.h.orig	2006-09-13 01:27:00.000000000 +0000
+++ include/schily/btorder.h
@@ -24,6 +24,9 @@
 #include <sys/types.h>			/* try to load isa_defs.h on Solaris */
 #define	_INCL_SYS_TYPES_H
 #endif
+#ifdef __NetBSD__
+#include <machine/endian.h>
+#endif
 
 #ifndef _SCHILY_MCONFIG_H
 #include <schily/mconfig.h>		/* load bit/byte-oder from xmconfig.h*/
@@ -104,7 +107,7 @@ error  Only one of _BIT_FIELDS_LTOH or _
 #	if defined(__ppc__) || defined(ppc) || defined(__ppc) || \
 	    defined(__PPC) || defined(powerpc) || defined(__powerpc__)
 
-#		if	defined(__BIG_ENDIAN__)
+#		if	defined(__BIG_ENDIAN__) || BYTE_ORDER == BIG_ENDIAN
 #			define _BIT_FIELDS_HTOL
 #		else
 #			define _BIT_FIELDS_LTOH
