$NetBSD: patch-ba,v 1.2 2006/05/16 17:08:36 thomasklausner Exp $

--- readcd/readcd.c.orig	2006-05-15 20:58:37.000000000 +0000
+++ readcd/readcd.c
@@ -431,12 +431,12 @@ main(ac, av)
 	/*
 	 * We don't need root privilleges anymore.
 	 */
-#ifdef	HAVE_SETREUID
-	if (setreuid(-1, getuid()) < 0)
-#else
 #ifdef	HAVE_SETEUID
 	if (seteuid(getuid()) < 0)
 #else
+#ifdef	HAVE_SETREUID
+	if (setreuid(-1, getuid()) < 0)
+#else
 	if (setuid(getuid()) < 0)
 #endif
 #endif
