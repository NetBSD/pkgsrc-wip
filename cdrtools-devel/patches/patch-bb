$NetBSD: patch-bb,v 1.5 2006/10/30 18:24:55 thomasklausner Exp $

--- mkisofs/mkisofs.c.orig	2006-10-29 12:50:11.000000000 +0000
+++ mkisofs/mkisofs.c
@@ -2035,12 +2035,12 @@ args_ok:
 		}
 	}
 	/* We don't need root privilleges anymore. */
-#ifdef	HAVE_SETREUID
-	if (setreuid(-1, getuid()) < 0)
-#else
 #ifdef	HAVE_SETEUID
 	if (seteuid(getuid()) < 0)
 #else
+#ifdef	HAVE_SETREUID
+	if (setreuid(-1, getuid()) < 0)
+#else
 	if (setuid(getuid()) < 0)
 #endif
 #endif
