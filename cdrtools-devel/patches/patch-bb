$NetBSD: patch-bb,v 1.6 2006/12/05 17:22:31 thomasklausner Exp $

--- mkisofs/mkisofs.c.orig	2006-12-02 19:50:02.000000000 +0000
+++ mkisofs/mkisofs.c
@@ -2117,12 +2117,12 @@ args_ok:
 		}
 	}
 	/* We don't need root privilleges anymore. */
-#ifdef	HAVE_SETREUID
-	if (setreuid(-1, getuid()) < 0)
-#else
 #ifdef	HAVE_SETEUID
 	if (seteuid(getuid()) < 0)
 #else
+#ifdef	HAVE_SETREUID
+	if (setreuid(-1, getuid()) < 0)
+#else
 	if (setuid(getuid()) < 0)
 #endif
 #endif
