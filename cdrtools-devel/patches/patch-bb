$NetBSD: patch-bb,v 1.3 2006/09/19 20:06:13 thomasklausner Exp $

--- mkisofs/mkisofs.c.orig	2006-09-15 15:04:56.000000000 +0000
+++ mkisofs/mkisofs.c
@@ -2162,12 +2162,12 @@ args_ok:
 		}
 	}
 	/* We don't need root privilleges anymore. */
-#ifdef	HAVE_SETREUID
-	if (setreuid(-1, getuid()) < 0)
-#else
 #ifdef	HAVE_SETEUID
 	if (seteuid(getuid()) < 0)
 #else
+#ifdef	HAVE_SETREUID
+	if (setreuid(-1, getuid()) < 0)
+#else
 	if (setuid(getuid()) < 0)
 #endif
 #endif
