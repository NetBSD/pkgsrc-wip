$NetBSD: patch-bb,v 1.4 2006/09/26 21:39:55 thomasklausner Exp $

--- mkisofs/mkisofs.c.orig	2006-09-24 13:22:59.000000000 +0000
+++ mkisofs/mkisofs.c
@@ -2166,12 +2166,12 @@ args_ok:
 		}
 	}
 	/* We don't need root privilleges anymore. */
-#ifdef	HAVE_SETREUID
-	if (setreuid(-1, getuid()) < 0)
-#else
 #ifdef	HAVE_SETEUID
 	if (seteuid(getuid()) < 0)
 #else
+#ifdef	HAVE_SETREUID
+	if (setreuid(-1, getuid()) < 0)
+#else
 	if (setuid(getuid()) < 0)
 #endif
 #endif
