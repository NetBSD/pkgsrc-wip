$NetBSD: patch-cf,v 1.3 2006/10/30 18:24:55 thomasklausner Exp $

--- mkisofs/tree.c.orig	2006-10-29 00:08:01.000000000 +0000
+++ mkisofs/tree.c
@@ -1523,7 +1523,7 @@ insert_file_entry(this_dir, whole_path, 
 	/*
 	 * >= is required by the large file summit standard.
 	 */
-	if (S_ISREG(lstatbuf.st_mode) && (lstatbuf.st_size >= (off_t)0x7FFFFFFF)) {
+	if (S_ISREG(lstatbuf.st_mode) && (lstatbuf.st_size >= (off_t)0x0FFFFFFFF)) {
 #endif
 #ifdef	EOVERFLOW
 		errno = EOVERFLOW;
