$NetBSD: patch-az,v 1.1.1.1 2010/09/08 15:00:56 rxg Exp $

--- chrome/browser/app_modal_dialog.h.orig	2010-08-06 09:04:49.000000000 +0000
+++ chrome/browser/app_modal_dialog.h
@@ -90,7 +90,7 @@ class AppModalDialog {
   virtual NativeDialog CreateNativeDialog() = 0;
 
   // A reference to the platform native dialog box.
-#if defined(OS_LINUX) || defined(OS_WIN)
+#if defined(OS_POSIX) && !defined(OS_MACOSX) || defined(OS_WIN)
   NativeDialog dialog_;
 #endif
 
