$NetBSD: patch-co,v 1.1.1.1 2010/09/08 15:00:58 rxg Exp $

--- chrome/browser/web_applications/web_app.cc.orig	2010-08-27 09:05:20.000000000 +0000
+++ chrome/browser/web_applications/web_app.cc
@@ -33,9 +33,9 @@
 #include "chrome/common/url_constants.h"
 #include "webkit/glue/dom_operations.h"
 
-#if defined(OS_LINUX)
+#if defined(OS_POSIX) && !defined(OS_MACOSX)
 #include "base/environment.h"
-#endif  // defined(OS_LINUX)
+#endif  // defined(OS_POSIX) && !defined(OS_MACOSX)
 
 #if defined(OS_WIN)
 #include "base/win_util.h"
@@ -259,7 +259,7 @@ void CreateShortcutTask::Run() {
 bool CreateShortcutTask::CreateShortcut() {
   DCHECK(ChromeThread::CurrentlyOn(ChromeThread::FILE));
 
-#if defined(OS_LINUX)
+#if defined(OS_POSIX) && !defined(OS_MACOSX)
   scoped_ptr<base::Environment> env(base::Environment::Create());
 
   std::string shortcut_template;
