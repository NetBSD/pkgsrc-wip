$NetBSD: patch-dg,v 1.1.1.1 2010/09/08 15:00:59 rxg Exp $

--- chrome/renderer/render_view.h.orig	2010-08-26 09:06:56.000000000 +0000
+++ chrome/renderer/render_view.h
@@ -979,7 +979,7 @@ class RenderView : public RenderWidget,
   // periodic timer so we don't send too many messages.
   void SyncNavigationState();
 
-#if defined(OS_LINUX)
+#if defined(OS_POSIX) && !defined(OS_MACOSX)
   void UpdateFontRenderingFromRendererPrefs();
 #else
   void UpdateFontRenderingFromRendererPrefs() {}
