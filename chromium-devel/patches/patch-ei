$NetBSD: patch-ei,v 1.1.1.1 2010/09/08 15:00:59 rxg Exp $

--- jingle/jingle.gyp.orig	2010-08-16 09:03:45.000000000 +0000
+++ jingle/jingle.gyp
@@ -82,7 +82,7 @@
         '../third_party/libjingle/libjingle.gyp:libjingle',
       ],
       'conditions': [
-        ['OS=="linux" or OS=="freebsd" or OS=="openbsd" or OS=="solaris"', {
+        ['OS=="linux" or OS=="freebsd" or OS=="netbsd" or OS=="openbsd" or OS=="solaris"', {
           'dependencies': [
             '../build/linux/system.gyp:gtk'
           ],
@@ -117,7 +117,7 @@
       # TODO(akalin): Remove this once we have our own test suite and
       # runner.
       'conditions': [
-        ['OS == "linux" or OS == "freebsd" or OS == "openbsd" or OS == "solaris"', {
+        ['OS == "linux" or OS == "freebsd" or OS == "netbsd" or OS == "openbsd" or OS == "solaris"', {
           'dependencies': [
             # Needed to handle the #include chain:
             #   base/test/test_suite.h
