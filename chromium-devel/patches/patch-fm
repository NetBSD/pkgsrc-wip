$NetBSD: patch-fm,v 1.1.1.1 2010/09/08 15:01:02 rxg Exp $

--- third_party/zlib/zlib.gyp.orig	2010-05-15 09:02:47.000000000 +0000
+++ third_party/zlib/zlib.gyp
@@ -5,12 +5,12 @@
 {
   'variables': {
     'conditions': [
-      [ 'OS=="linux" or OS=="freebsd"', {
+      [ 'OS=="linux" or OS=="freebsd" or OS=="netbsd"', {
         # Link to system .so since we already use it due to GTK.
         # TODO(pvalchev): OpenBSD is purposefully left out, as the system
         # zlib brings up an incompatibility that breaks rendering.
         'use_system_zlib%': 1,
-      }, {  # OS!="linux" and OS!="freebsd"
+      }, {  # OS!="linux" and OS!="freebsd" and OS!="netbsd"
         'use_system_zlib%': 0,
       }],
     ],
