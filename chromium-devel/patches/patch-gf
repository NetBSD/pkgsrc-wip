$NetBSD: patch-gf,v 1.1.1.1 2010/09/08 15:01:02 rxg Exp $

--- third_party/WebKit/WebCore/platform/graphics/chromium/FontCustomPlatformData.h.orig	2010-08-11 09:09:11.000000000 +0000
+++ third_party/WebKit/WebCore/platform/graphics/chromium/FontCustomPlatformData.h
@@ -39,7 +39,7 @@
 #if OS(WINDOWS)
 #include "PlatformString.h"
 #include <windows.h>
-#elif OS(LINUX)
+#elif OS(LINUX) || OS(FREEBSD) || OS(NETBSD)
 #include "SkTypeface.h"
 #endif
 
@@ -54,7 +54,7 @@ struct FontCustomPlatformData : Noncopya
         : m_fontReference(fontReference)
         , m_name(name)
     {}
-#elif OS(LINUX)
+#elif OS(LINUX) || OS(FREEBSD) || OS(NETBSD)
     explicit FontCustomPlatformData(SkTypeface* typeface)
         : m_fontReference(typeface)
     {}
@@ -70,7 +70,7 @@ struct FontCustomPlatformData : Noncopya
 #if OS(WINDOWS)
     HANDLE m_fontReference;
     String m_name;
-#elif OS(LINUX)
+#elif OS(LINUX) || OS(FREEBSD) || OS(NETBSD)
     SkTypeface* m_fontReference;
 #endif
 };
