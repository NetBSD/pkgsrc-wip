$NetBSD: patch-gh,v 1.1.1.1 2010/09/08 15:01:02 rxg Exp $

--- third_party/WebKit/WebCore/platform/graphics/skia/PlatformContextSkia.h.orig	2010-08-29 09:08:32.000000000 +0000
+++ third_party/WebKit/WebCore/platform/graphics/skia/PlatformContextSkia.h
@@ -95,7 +95,7 @@ public:
     // |rect|. This layer is implicitly restored when the next restore is
     // invoked.
     // NOTE: |imageBuffer| may be deleted before the |restore| is invoked.
-#if OS(LINUX) || OS(WINDOWS)
+#if OS(LINUX) || OS(FREEBSD) || OS(NETBSD) || OS(WINDOWS)
     void beginLayerClippedToImage(const FloatRect&, const ImageBuffer*);
 #endif
     void clipPathAntiAliased(const SkPath&);
@@ -192,7 +192,7 @@ public:
     void syncSoftwareCanvas() const;
 
 private:
-#if OS(LINUX) || OS(WINDOWS)
+#if OS(LINUX) || OS(FREEBSD) || OS(NETBSD) || OS(WINDOWS)
     // Used when restoring and the state has an image clip. Only shows the pixels in
     // m_canvas that are also in imageBuffer.
     void applyClipFromImage(const FloatRect&, const SkBitmap&);
