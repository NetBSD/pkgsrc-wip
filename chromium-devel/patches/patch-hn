$NetBSD: patch-hn,v 1.1 2010/09/26 16:22:24 rxg Exp $

--- base/file_util_posix.cc.orig	2010-09-13 15:18:37.000000000 +0000
+++ base/file_util_posix.cc
@@ -58,7 +58,7 @@ bool RealPath(const FilePath& path, File
 
 }  // namespace
 
-#if defined(OS_OPENBSD) || defined(OS_FREEBSD) || \
+#if defined(OS_OPENBSD) || defined(OS_FREEBSD) || defined(OS_NETBSD) || \
     (defined(OS_MACOSX) && \
      MAC_OS_X_VERSION_MIN_REQUIRED < MAC_OS_X_VERSION_10_5)
 typedef struct stat stat_wrapper_t;
@@ -94,7 +94,7 @@ int CountFilesCreatedAfter(const FilePat
   DIR* dir = opendir(path.value().c_str());
   if (dir) {
 #if !defined(OS_LINUX) && !defined(OS_MACOSX) && !defined(OS_FREEBSD) && \
-    !defined(OS_OPENBSD) && !defined(OS_SOLARIS)
+    !defined(OS_NETBSD) && !defined(OS_OPENBSD) && !defined(OS_SOLARIS)
   #error Port warning: depending on the definition of struct dirent, \
          additional space for pathname may be needed
 #endif
@@ -672,7 +672,7 @@ bool FileEnumerator::ReadDirectory(std::
     return false;
 
 #if !defined(OS_LINUX) && !defined(OS_MACOSX) && !defined(OS_FREEBSD) && \
-    !defined(OS_OPENBSD) && !defined(OS_SOLARIS)
+    !defined(OS_NETBSD) && !defined(OS_OPENBSD) && !defined(OS_SOLARIS)
   #error Port warning: depending on the definition of struct dirent, \
          additional space for pathname may be needed
 #endif
