$NetBSD: patch-cv,v 1.1.1.1 2010/11/27 01:24:42 rxg Exp $

--- chrome/test/webdriver/session_manager.cc.orig	2010-10-15 08:02:37.000000000 +0000
+++ chrome/test/webdriver/session_manager.cc
@@ -64,7 +64,7 @@ bool SessionManager::SetIPAddress(const 
   for (int i = 0; i < 10; ++i) {
 #ifdef OS_MACOSX
     snprintf(buff, sizeof(buff), "%s%d", "en", i);
-#elif OS_LINUX
+#else
     snprintf(buff, sizeof(buff), "%s%d", "eth", i);
 #endif
     addr_ = IPLookup(std::string(buff));
