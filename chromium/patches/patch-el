$NetBSD: patch-el,v 1.1.1.1 2010/11/27 01:24:43 rxg Exp $

--- third_party/WebKit/JavaScriptCore/config.h.orig	2010-09-13 15:28:38.000000000 +0000
+++ third_party/WebKit/JavaScriptCore/config.h
@@ -57,7 +57,7 @@
 
 #endif
 
-#if OS(FREEBSD) || OS(OPENBSD)
+#if OS(FREEBSD) || OS(OPENBSD) || OS(NETBSD) || OS(DRAGONFLY)
 #define HAVE_PTHREAD_NP_H 1
 #endif
 
