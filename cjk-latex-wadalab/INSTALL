#!/bin/sh
#

STAGE=$2

MAPDIR=@MAPDIR@

case ${STAGE} in
    PRE-INSTALL)
	;;
    POST-INSTALL)
	updmap --enable Map ${MAPDIR}/wadalab.map
	;;
    *)
	echo "Unexpected argument: ${STAGE}"
	exit 1
	;;
esac
exit 0
