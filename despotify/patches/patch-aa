$NetBSD: patch-aa,v 1.2 2010/02/02 18:17:19 tnn2 Exp $

--- clients/despotify/local.mk.orig	2010-02-02 17:50:11.000000000 +0000
+++ clients/despotify/local.mk
@@ -17,7 +17,7 @@ ifeq ($(shell uname -s),Darwin)
     # The default ncurses library on Mac OS X supports wide characters
     # so force linking with the one in /usr/lib
     LDFLAGS = -L/usr/lib -lncurses -framework CoreAudio
-else ifeq ($(shell uname -s),Linux)
+else ifeq ($(LINUX_BACKEND),libao)
     DESPOTIFY_OBJS += libao.o
     LDFLAGS += -lncursesw -lao
 else
