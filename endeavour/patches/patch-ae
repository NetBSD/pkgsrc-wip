$NetBSD: patch-ae,v 1.2 2006/08/03 22:06:30 dhowland Exp $

--- endeavour2/fb.c.orig	2006-07-01 20:44:12.000000000 -0400
+++ endeavour2/fb.c
@@ -14,7 +14,7 @@
 # if defined(__SOLARIS__)
 #  include <sys/mnttab.h>
 #  include <sys/vfstab.h>
-# elif defined(__FreeBSD__)
+# elif defined(__FreeBSD__)||defined(__NetBSD__)
 /* #  include <mntent.h> */
 # else
 #  include <mntent.h>
@@ -854,7 +854,7 @@ static gchar **FileBrowserGetDrivePaths(
 	    *n = strc;
 
 	return(strv);
-#elif defined(__FreeBSD__)
+#elif defined(__FreeBSD__)||defined(__NetBSD__)
 	/* FreeBSD */
 	if(n != NULL )
 	    *n = 0;
