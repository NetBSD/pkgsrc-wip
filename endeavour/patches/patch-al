$NetBSD: patch-al,v 1.1 2008/03/11 02:36:59 dhowland Exp $

--- endeavour2/mime_type_edit_dlg.c.orig	2008-02-02 17:00:00.000000000 -0500
+++ endeavour2/mime_type_edit_dlg.c
@@ -3143,9 +3143,7 @@ static CmdDlg *CmdDlgNew(
 	parent5 = w;
 
 	w = gtk_label_new(
-"Remember to put double quotes (\") around tokens so that the\n\
-substituted values which have spaces don't get mistaken for
-multiple arguments (ie \"%paths\")."
+"Remember to put double quotes (\") around tokens so that the\nsubstituted values which have spaces don't get mistaken for multiple arguments (ie \"%paths\")."
 	);
 	gtk_label_set_justify(GTK_LABEL(w), GTK_JUSTIFY_LEFT);
 	gtk_box_pack_start(GTK_BOX(parent5), w, FALSE, FALSE, 0);
