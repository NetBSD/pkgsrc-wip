- Thread bug fixed for curses version only, GTK still buggy.
- Does not support PlugIns under NetBSD yet. I assume this is
because ettercap doesn't see that we don't need a library for dlopen()
under NetBSD.

