$NetBSD: patch-ac,v 1.1 2005/12/15 14:37:50 ghen Exp $

--- cache_file.c.orig	2001-04-04 17:12:02.000000000 +0000
+++ cache_file.c
@@ -43,11 +43,11 @@
 #include <cache_file.h>
 
 #if HAVE_STRERROR
-extern int errno;
+#  include <errno.h>
 #  define error_string strerror(errno)
 #elif HAVE_SYS_ERRLIST
 extern const char *const sys_errlist[];
-extern int errno;
+#  include <errno.h>
 #  define error_string (sys_errlist[errno])
 #else
 #  define error_string "error message not found"
@@ -63,11 +63,11 @@ extern int errno;
 #  define dprintf(x)
 #endif
 #if HAVE_STRERROR
-extern int errno;
+#  include <errno.h>
 #  define error_string strerror(errno)
 #elif HAVE_SYS_ERRLIST
 extern const char *const sys_errlist[];
-extern int errno;
+#  include <errno.h>
 #  define error_string (sys_errlist[errno])
 #else
 #  define error_string "error message not found"
