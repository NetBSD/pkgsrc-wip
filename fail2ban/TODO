* Install default config files in $PREFIX/share/examples

* Install a copy in $PREFIX/etc/fail2ban

* Use above for config

* deal with /var/run/fail2ban not existing somehow

* ? Provide a more sane default config

* This package has known vulnerabilities, please investigate and fix
  if possible. (see: CVE-2012-5642)
