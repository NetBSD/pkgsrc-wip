$NetBSD: patch-SConstruct,v 1.2 2013/01/28 12:41:42 othyro Exp $

Make SConstruct use $PATH.

--- SConstruct.orig	2012-11-27 17:07:46.000000000 +0000
+++ SConstruct
@@ -32,6 +32,8 @@ AddOption('--prefix', dest='prefix', typ
 prefix = GetOption('prefix')
 env = Environment(options = opts)
 
+env['ENV']['PATH'] = os.environ.get('PATH')
+
 #### Uncomment this for a public release ###
 env.Append(CPPDEFINES=["PUBLIC_RELEASE"])
 env['DEBUG'] = 0
