$NetBSD$

--- src/common/classes/semaphore.h.orig	2007-09-04 07:51:39.000000000 +0000
+++ src/common/classes/semaphore.h
@@ -72,7 +72,7 @@ public:
 
 #ifdef MULTI_THREAD
 
-#ifdef DARWIN
+#if defined(DARWIN) || defined(NETBSD)
 
 #include <pthread.h>
 #include <errno.h>
