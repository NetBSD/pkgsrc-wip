$NetBSD: patch-ml,v 1.1 2009/06/30 19:51:08 tnn2 Exp $

--- js/src/Makefile.in.orig	2009-06-16 00:42:43.000000000 +0200
+++ js/src/Makefile.in
@@ -429,6 +429,9 @@ endif # WINNT
 ifeq ($(OS_ARCH),FreeBSD)
 EXTRA_LIBS	+= -pthread
 endif
+ifeq ($(OS_ARCH),DragonFly)
+EXTRA_LIBS	+= -pthread
+endif
 ifeq ($(OS_ARCH),IRIX)
 ifdef USE_N32
 DASH_R		+= -n32
