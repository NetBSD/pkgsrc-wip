$NetBSD: patch-aa,v 1.2 2009/08/10 14:52:39 jakllsch Exp $

--- Makefile.orig	2009-05-03 23:37:07.000000000 +0000
+++ Makefile
@@ -26,6 +26,10 @@ ifeq ($(OS_ARCH), FreeBSD)
 CFLAGS += -I/usr/local/include
 LDFLAGS += -L/usr/local/lib
 endif
+ifeq ($(OS_ARCH), NetBSD)
+OS_MACHINE = $(shell uname -m)
+LDFLAGS += -l$(OS_MACHINE)
+endif
 
 LDFLAGS += -lpci -lz
 
