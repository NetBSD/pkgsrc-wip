$NetBSD: patch-ab,v 1.2 2006/11/26 22:49:57 larnor Exp $

--- libgaim/dnssrv.c.orig	2006-11-10 08:24:19.000000000 +0100
+++ libgaim/dnssrv.c
@@ -39,6 +39,16 @@
 #endif
 #endif
 
+#ifndef NS_GET16
+#define NS_GET16(s, cp) { \
+       register u_char *t_cp = (u_char *)(cp); \
+       (s) = ((u_int16_t)t_cp[0] << 8) \
+           | ((u_int16_t)t_cp[1]) \
+           ; \
+       (cp) += 2; \
+}
+#endif
+
 #include "dnssrv.h"
 #include "eventloop.h"
 #include "debug.h"
