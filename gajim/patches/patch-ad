--- src/common/configpaths.py.orig	2007-04-10 10:43:42.000000000 +0200
+++ src/common/configpaths.py	2007-04-10 10:42:45.000000000 +0200
@@ -86,7 +86,7 @@
 	for n, p in zip(k, v):
 		paths.add_from_root(n, p)
 
-	paths.add('DATA', os.path.join(u'..', windowsify(u'data')))
+	paths.add('DATA', os.path.join(u'@PREFIX@/share/gajim', windowsify(u'data')))
 	paths.add('HOME', fse(os.path.expanduser('~')))
 	paths.add('TMP', fse(tempfile.gettempdir()))
 
