$NetBSD: patch-ad,v 1.1.1.1 2003/04/03 15:09:28 cubidou Exp $

--- src/sysdeps/bsd-net-open.c.orig	2003-01-09 02:02:44.000000000 +0100
+++ src/sysdeps/bsd-net-open.c
@@ -83,7 +83,11 @@ gkrellm_sys_disk_read_data(void)
 	  * d.xfer value (number of transfers) but there a scaling problem:
 	  * d.xfer is usually much more smaller thant d.dk_bytes... */
 
+#if __NetBSD_Version__ >= 106110000
+         gkrellm_disk_assign_data_by_name(buf, d.dk_rbytes, d.dk_wbytes);
+#else
 	 gkrellm_disk_assign_data_by_name(buf, d.dk_bytes, 0);
+#endif
       }
    }
 }
