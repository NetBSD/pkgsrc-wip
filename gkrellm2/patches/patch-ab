$NetBSD: patch-ab,v 1.2 2003/06/07 12:53:37 cubidou Exp $

--- src/Makefile.orig	2003-06-03 18:50:12.000000000 +0200
+++ src/Makefile
@@ -12,7 +12,7 @@ MANMODE ?= 644
 MANDIRMODE ?= 755
 INSTALL ?= install
 LINK_FLAGS ?= -Wl,-E
-SMC_LIBS ?= -L/usr/X11R6/lib -lSM -lICE
+SMC_LIBS ?= -L$(X11BASE)/lib -Wl,-R$(X11BASE)/lib -lSM -lICE
 CC ?= gcc
 STRIP ?= -s
 
@@ -84,7 +84,7 @@ solaris:
 		SYS_LIBS="-lkstat -lkvm -ldevinfo" gkrellm
 
 install:
-	$(INSTALL) -c $(STRIP) -m $(BINMODE) gkrellm $(INSTALLDIR)/$(PACKAGE)
+	$(INSTALL) -c $(STRIP) -m $(BINMODE) -g $(BINGROUP) gkrellm $(INSTALLDIR)/$(PACKAGE)
 	$(INSTALL) -d -m $(INCLUDEDIRMODE) $(INCLUDEDIR)/gkrellm2
 	$(INSTALL) -c -m $(INCLUDEMODE) $(GKRELLM_INCLUDES) $(INCLUDEDIR)/gkrellm2
 	$(INSTALL) -d -m $(MANDIRMODE) $(MANDIR)
@@ -100,8 +100,8 @@ install_freebsd:
 
 install_netbsd:
 	$(MAKE) install
-	chgrp kmem $(INSTALLDIR)/$(PACKAGE)
-	chmod g+s $(INSTALLDIR)/$(PACKAGE)
+#	chgrp kmem $(INSTALLDIR)/$(PACKAGE)
+#	chmod g+s $(INSTALLDIR)/$(PACKAGE)
 
 install_openbsd:
 	$(MAKE) install
