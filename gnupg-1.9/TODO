Works when tested by itself.

Need to fix test for pth. As supplied in gnupg,
it detects pth on NetBSD even though pth is not
installed. For now, the test is bypassed so pth
is never found. This need to be changed.
