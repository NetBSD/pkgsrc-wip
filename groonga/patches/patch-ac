$NetBSD: patch-ac,v 1.1 2010/08/08 12:54:17 obache Exp $

need for EAI_* in netdb.h

--- lib/groonga_in.h.orig	2010-07-29 04:53:55.000000000 +0000
+++ lib/groonga_in.h
@@ -32,8 +32,12 @@
 #define _XOPEN_SOURCE 600
 #else
 #if !defined(__APPLE__) && !defined(__FreeBSD__)
+#if defined(__NetBSD__)
+#define _XOPEN_SOURCE 520
+#else
 #define _XOPEN_SOURCE 500
 #endif
+#endif
 #endif /* USE_AIO */
 
 #ifdef HAVE_STDLIB_H
