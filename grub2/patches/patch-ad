$NetBSD: patch-ad,v 1.6 2009/12/16 14:37:31 gregoire Exp $

--- conf/common.mk.orig	2009-11-09 16:48:24.000000000 +0100
+++ conf/common.mk
@@ -701,9 +701,9 @@ CLEANFILES += update-grub_lib
 	./config.status --file=$@:$<
 	chmod +x $@
 grub-mkconfig_SCRIPTS = 00_header 30_os-prober 40_custom
-ifneq (, $(host_kernel))
-grub-mkconfig_SCRIPTS += 10_$(host_kernel)
-endif
+#ifneq (, $(host_kernel))
+grub-mkconfig_SCRIPTS += 10_freebsd 10_hurd 10_linux 10_windows
+#endif
 
 CLEANFILES += $(grub-mkconfig_SCRIPTS)
 
