$NetBSD: patch-ao,v 1.1 2009/12/16 14:37:31 gregoire Exp $

--- util/update-grub_lib.in.orig	2009-11-09 16:48:16.000000000 +0100
+++ util/update-grub_lib.in
@@ -17,7 +17,8 @@
 prefix=@prefix@
 exec_prefix=@exec_prefix@
 libdir=@libdir@
+pkglibdirbase=${libdir}/`echo @PACKAGE_TARNAME@ | sed "@program_transform_name@"`
 
-. ${libdir}/grub/grub-mkconfig_lib
+. ${pkglibdirbase}/grub-mkconfig_lib
 
 grub_warn "update-grub_lib is deprecated, use grub-mkconfig_lib instead"
