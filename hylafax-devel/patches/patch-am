$NetBSD: patch-am,v 1.2 2007/03/07 09:56:21 ahallmann Exp $

--- faxd/UUCPLock.c++.orig	2000-09-21 11:24:54.000000000 +0200
+++ faxd/UUCPLock.c++
@@ -37,6 +37,7 @@ extern "C" {
 }
 #endif
 #include <pwd.h>
+#include <grp.h>
 
 /*
  * UUCP Device Locking Support.
@@ -146,8 +147,13 @@ UUCPLock::setupIDs()
 	if (!pwd)
 	    faxApp::fatal("Can not deduce identity of UUCP");
 	UUCPuid = pwd->pw_uid;
-	UUCPgid = pwd->pw_gid;
 	endpwent();			// paranoia
+
+	const group *grp = getgrnam(FAX_GROUP);
+	if (!grp)
+	    faxApp::fatal("Can not deduce group identity of '" FAX_GROUP "'");
+	UUCPgid = grp->gr_gid;
+	endgrent();			// paranoia
     }
 }
 uid_t UUCPLock::getUUCPUid() { setupIDs(); return UUCPuid; }
