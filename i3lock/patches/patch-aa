$NetBSD: patch-aa,v 1.1.1.1 2010/01/04 13:53:51 pallegra Exp $

--- config.mk.orig	2009-05-10 17:50:59.000000000 +0000
+++ config.mk
@@ -4,22 +4,20 @@ VERSION = 1.0
 # Customize below to fit your system
 
 # paths
-PREFIX = /usr
+X11INC = ${X11BASE}/include
+X11LIB = ${X11BASE}/lib
 
-X11INC = /usr/X11R6/include
-X11LIB = /usr/X11R6/lib
-
-MANDIR = $(DESTDIR)$(PREFIX)/share/man
+MANDIR = $(DESTDIR)$(PREFIX)/${PKGMANDIR}
 
 # includes and libs
 INCS = -I. -I/usr/include -I${X11INC}
-LIBS = -L${X11LIB} -lX11 -lpam -lXext
+LIBS = -lX11 -lpam -lXext
 
 # flags
-CPPFLAGS = -DVERSION=\"${VERSION}\"
-CFLAGS = -std=c99 -pedantic -Wall -Os ${INCS} ${CPPFLAGS}
-LDFLAGS = -s ${LIBS}
+CPPFLAGS += -DVERSION=\"${VERSION}\"
+CFLAGS += -std=c99 -pedantic -Wall ${CPPFLAGS}
+LDFLAGS += ${LIBS}
 
 # compiler and linker
-CC = cc
-INSTALL=install
+#CC = cc
+#INSTALL=install
