$NetBSD: patch-ac,v 1.2 2009/04/03 21:50:51 alnsn Exp $

--- icc10/bin/profrun.orig	2009-04-02 20:01:25.000000000 +0100
+++ icc10/bin/profrun
@@ -28,11 +28,4 @@ fi
 export PROFRUN_MODIFIED_PATH="${PATH}"
 export PROFRUN_MODIFIED_LD_LIBRARY_PATH="${LD_LIBRARY_PATH}"
 
-BINARY="profrun.bin"
-
-if [ $# != 0 ]
-then
-    exec -a "${BINARY}" "${BINARY}" "$@"
-else
-    exec -a "${BINARY}" "${BINARY}"
-fi
+exec profrun.bin "$@";
