$NetBSD: patch-aj,v 1.1.1.1 2003/04/03 09:27:17 cubidou Exp $

--- scripts/Makefile.orig	2003-03-27 20:40:15.000000000 +0100
+++ scripts/Makefile
@@ -25,7 +25,8 @@ _install:
 	done
 
 ion: ion.in
-	sed 's#PREFIX#$(PREFIX)#g' $< > $@
+	sed -e 's#ETCDIR#$(ETCDIR)#g' -e 's#LIBDIR#$(LIBDIR)#g' \
+	    -e 's#BINDIR#$(BINDIR)#g' $< > $@
 	chmod $(BIN_MODE) $@
 	
 pwm: pwm.in
