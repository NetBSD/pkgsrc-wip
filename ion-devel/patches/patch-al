$NetBSD: patch-al,v 1.1.1.1 2003/04/03 09:27:17 cubidou Exp $

--- ioncore/Makefile.orig	2003-03-27 20:40:15.000000000 +0100
+++ ioncore/Makefile
@@ -67,4 +67,7 @@ endif
 _install:
 	$(INSTALLDIR) $(BINDIR)
 	$(INSTALL) -m $(BIN_MODE) ioncore $(BINDIR)
-	# $(STRIP) $(BINDIR)/ioncore
+	$(STRIP) $(BINDIR)/ioncore
+	$(INSTALLDIR) $(INCDIR)/ion
+	$(INSTALLDIR) $(INCDIR)/ion/ioncore
+	$(INSTALL) -m $(DATA_MODE) *.h $(INCDIR)/ion/ioncore
