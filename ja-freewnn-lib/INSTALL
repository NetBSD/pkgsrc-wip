# $NetBSD: INSTALL,v 1.1.1.1 2011/06/01 14:32:11 makoto Exp $

case ${STAGE} in
POST-INSTALL)
	for i in `${FIND} ${PREFIX}/${WNNDICDIR} -type f \( -name \*dic -o -name  \*fzk \)`; 
	 do
	   ${CHMOD} +w ${i};
	   ${PREFIX}/bin/Wnn4/wnntouch ${i};
	 done;
	;;
esac
