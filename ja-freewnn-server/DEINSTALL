# $NetBSD: DEINSTALL,v 1.1 2011/06/02 00:40:28 makoto Exp $

case ${STAGE} in
POST-DEINSTALL)
	${FIND} ${PREFIX}/${WNNDICDIR} -type f \( -name \*dic -o -name  \*fzk \) | xargs rm -f
#	for i in `${FIND} ${PREFIX}/${WNNDICDIR} -type f \( -name \*dic -o -name  \*fzk \)`; 
#	 do
#	   ${CHMOD} +w ${i};
#	   ${RM} -f ${i};
#	 done;
	;;
esac
