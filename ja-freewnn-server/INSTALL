# $NetBSD: INSTALL,v 1.1 2011/06/02 00:40:28 makoto Exp $

case ${STAGE} in
POST-INSTALL)
	for i in `${FIND} ${PREFIX}/${WNNDICDIR} -type f \( -name \*dic -o -name  \*fzk \)`; 
	 do
	   ${CHMOD} +w ${i};
	   ${PREFIX}/bin/wnntouch ${i};
	 done;
	;;
esac
