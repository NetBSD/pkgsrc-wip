$NetBSD$

Replace git dependency with vendored path for mermaid-rs-renderer.

--- crates/jcode-tui-mermaid/Cargo.toml.orig
+++ crates/jcode-tui-mermaid/Cargo.toml
@@ -19,7 +19,7 @@
 dirs = "5"
 image = { version = "0.25", default-features = false, features = ["png", "jpeg"] }
 jcode-tui-workspace = { path = "../jcode-tui-workspace" }
-mermaid-rs-renderer = { git = "https://github.com/1jehuang/mermaid-rs-renderer.git", tag = "v0.3.1", optional = true }
+mermaid-rs-renderer = { path = "../../../vendor/mermaid-rs-renderer-0.3.1", optional = true }
 ratatui = "0.30"
 ratatui-image = { version = "10.0.6", default-features = false, features = ["crossterm"] }
 resvg = "0.46"
