$NetBSD: patch-ac,v 1.1.1.1 2003/04/07 16:16:07 tvierling Exp $

--- src/bsd/hpi/green_threads/src/signals.c.orig	Mon Feb 17 11:40:19 2003
+++ src/bsd/hpi/green_threads/src/signals.c
@@ -636,7 +636,11 @@ green_sigprocmask(int how, const sigset_
         void *dlMain = dlopen("/usr/lib/libc.so", RTLD_LAZY);
 #endif
 #endif
+#ifdef __NetBSD__
+        func = (fn_t *)dlsym(dlMain, CNAME("__sigprocmask14"));
+#else
         func = (fn_t *)dlsym(dlMain, CNAME("sigprocmask"));
+#endif
 #else
         func = (fn_t *)dlsym(RTLD_NEXT, "_sigprocmask");
 #endif
