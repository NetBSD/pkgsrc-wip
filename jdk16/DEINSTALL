# $NetBSD: DEINSTALL,v 1.1 2011/07/23 17:38:49 ftigeot Exp $

MACHINE_ARCH=`uname -m`

case ${STAGE} in
DEINSTALL)
	if [ "${MACHINE_ARCH}" = "i386" ]; then
		${RM} -f @JAVA_HOME@/lib/@MACHINE_ARCH@/client/classes.jsa;;
	fi
	;;
esac
