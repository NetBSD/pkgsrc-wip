- File patches upstream.
  - libdir64
  - visibility defines (recheck if really needed)
  - errno.h

- Address ctype(3) UB with upstream.

- Read install instructions.

- Add options for at least pgsql.

- Test.
