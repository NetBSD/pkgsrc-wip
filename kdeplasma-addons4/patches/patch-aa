$NetBSD: patch-aa,v 1.1 2009/01/10 10:39:38 mwdavies Exp $

--- applets/lancelot/libs/lancelot/widgets/BasicWidget.cpp.orig	2008-08-21 07:53:49.000000000 +1200
+++ applets/lancelot/libs/lancelot/widgets/BasicWidget.cpp
@@ -27,6 +27,10 @@
 
 #define max(A, B) ((A) >= (B)) ? (A) : (B)
 
+#ifdef __NetBSD__
+#define fmaxf(A, B) ((A) >= (B)) ? (A) : (B)
+#endif
+
 // macro for setting the left coordinate of items
 // relative to the parent and with alignment
 // taken into consideration
