# $NetBSD: texmf-deinstall.tmpl,v 1.3 2008/12/13 08:53:05 minskim Exp $
#
# Rebuild ls-R databases.
#

case ${STAGE} in
POST-DEINSTALL)
	@FIND@ -d @TEXMF_DIRS@ -type d -empty -exec @RMDIR@ {} \;
	@MKTEXLSR@ @TEXMF_DIRS@
        ;;
*)
	;;
esac
