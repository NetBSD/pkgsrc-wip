$NetBSD: patch-aa,v 1.2 2003/09/26 06:26:48 xtraeme Exp $

--- src/modules/snd/libkvisnd.cpp.orig	2003-09-25 19:57:14.000000000 +0200
+++ src/modules/snd/libkvisnd.cpp	2003-09-25 19:58:19.000000000 +0200
@@ -49,7 +49,13 @@
 	#ifdef COMPILE_OSS_SUPPORT
 		#include <fcntl.h>
 		#include <sys/ioctl.h>
+#ifdef __Linux__
 		#include <linux/soundcard.h>
+#elif defined(__FreeBSD__) && (__FreeBSD_version >= 500000)
+		#include <sys/soundcard.h>
+#else
+		#include <soundcard.h>
+#endif
 		#ifdef COMPILE_AUDIOFILE_SUPPORT
 			#include <audiofile.h>
 		#endif //COMPILE_AUDIOFILE_SUPPORT
