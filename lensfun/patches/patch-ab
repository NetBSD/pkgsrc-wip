$NetBSD: patch-ab,v 1.1.1.1 2008/09/08 14:50:32 thomasklausner Exp $

--- build/mak/host/posix.mak.orig	2007-12-24 14:24:18.000000000 +0000
+++ build/mak/host/posix.mak
@@ -11,6 +11,6 @@ endef
 # $1 - directory, $2 - target dir, $3 - mode for files
 define SINGLE.INSTALLDIR
 	if [ ! -d $2 ]; then install -m 0755 -d $2; else rm -rf $2$(notdir $(1:/=)); fi
-	cp -drP $1 $2$(notdir $(1:/=))
+	cp -r $1 $2$(notdir $(1:/=))
 	find $2$(notdir $(1:/=)) -type f -print0 | xargs -0 chmod $3
 endef
