Currently this package only works in firefox if you preload libGL
ie: LD_PRELOAD=/usr/X11R7/lib/libGL.so firefox

The stand-alone player has no problems.

wiz 20140216:
doesn't build for me on NetBSD-6.99.31. Configuring fails with:

-- Looking for C++ include atomic
-- Looking for C++ include atomic - not found
CMake Error at src/CMakeLists.txt:159 (INSTALL):
  install TARGETS given no ARCHIVE DESTINATION for static library target
  "spark".
