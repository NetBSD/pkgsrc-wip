20031027 PA 
	-check ghostscript dependencies (we need ps2pdf) for all ghostscript
	  flavours
	-check {lilypond,music-glossary,lilypond-internals}.info
	  'info lilypond' show manpage instead of (registered) 'Gnu lilypond'
	  infopages
	-resolve pyc/pyo deinstall problem (they exist when lilypond was called
	  as root)
 buildlink3 dependency for ghostscript installs -gnu, thats fine (PA)

20031024 PA test build/install/deinstall/package process
 seems ok on stock 1.6.1 (PA)
