$NetBSD: patch-ac,v 1.1.1.1 2003/06/12 05:50:37 cubidou Exp $

--- src/sdl.c.orig	2002-08-08 04:37:50.000000000 +0200
+++ src/sdl.c
@@ -173,6 +173,8 @@ SFnt* SF_Ld(char *fname)
 	
     //table
     file = fopen(path, "r");
+    if (file == NULL)
+        err(1, "SF_Ld: %s", path);
     fseek(file, -1, SEEK_END);
     fread(&fnt->off, 1, 1, file);
 #ifdef DEBUG
