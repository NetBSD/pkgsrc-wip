$NetBSD: patch-aa,v 1.1.1.2 2010/08/06 20:28:48 ahp-nils Exp $

--- Makefile.orig	2008-05-19 10:25:54.000000000 +0000
+++ Makefile
@@ -45,6 +45,14 @@ ifeq ($(OS_NAME),SunOS)
     endif
 endif
 
+# NetBSD
+ifeq ($(OS_NAME),NetBSD)
+    CFLAGS += -I/usr/include
+    CFLAGS += -I$(BASEDIR)/include
+    LOADLIBES += -L/usr/lib
+    LOADLIBES += -L$(BASEDIR)/lib -Wl,-R,$(BASEDIR)/lib
+endif
+
 # Red Hat Linux
 ifeq ($(OS_NAME),Linux)
     INSTALL = install
