$NetBSD: patch-ae,v 1.3 2010/11/06 15:05:50 ahp-nils Exp $

Add pkgsrc paths compatibility

--- examples/logrotate-default.orig	2010-06-28 08:04:56.000000000 +0000
+++ examples/logrotate-default
@@ -12,18 +12,12 @@ create
 dateext
 
 # uncomment this if you want your log files compressed
-#compress
+compress
 
-# RPM packages drop log rotation information into this directory
-include /etc/logrotate.d
+# Packages drop log rotation information into this directory
+include __PREFIX__/etc/logrotate.d
 
-# no packages own wtmp and btmp -- we'll rotate them here
-/var/log/wtmp {
-    monthly
-    create 0664 root utmp
-	minsize 1M
-    rotate 1
-}
+# no packages own btmp -- we'll rotate it here
 
 /var/log/btmp {
     missingok
