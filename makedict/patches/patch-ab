$NetBSD: patch-ab,v 1.1.1.1 2009/09/20 16:54:46 cheusov Exp $

--- src/charset_conv.cpp.orig	2008-04-28 20:39:42.000000000 +0300
+++ src/charset_conv.cpp
@@ -66,7 +66,7 @@ bool CharsetConv::convert(const char *st
 again:
 	err = iconv(cd_,
 //this need because win32 version of iconv and from glibc is different
-#ifdef WIN32
+#if defined(WIN32) || defined(__NetBSD__)
 		    (const char **)&p,
 #else
 		    (char **)&p,
