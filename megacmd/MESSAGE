===========================================================================
$NetBSD$
$NetBSD: MESSAGE,

To use megacmd features need to generate machine-id.
Run as root: dbus-uuidgen --ensure

===========================================================================
