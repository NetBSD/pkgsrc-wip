$NetBSD: patch-ad,v 1.1.1.1 2003/07/30 17:08:55 jeremy-c-reed Exp $

--- appl/telnet/telnet/Makefile.in.orig	Thu Aug 29 02:10:06 2002
+++ appl/telnet/telnet/Makefile.in
@@ -54,11 +54,10 @@ clean::
 install::
 	for f in telnet; do \
 	  $(INSTALL_PROGRAM) $$f \
-		$(DESTDIR)$(CLIENT_BINDIR)/`echo $$f|sed '$(transform)'`; \
+		$(DESTDIR)$(CLIENT_BINDIR)/`echo $$f|sed 's,^,k,'`; \
 	  $(INSTALL_DATA) $(srcdir)/$$f.1 \
-		${DESTDIR}$(CLIENT_MANDIR)/`echo $$f|sed '$(transform)'`.1; \
+		${DESTDIR}$(CLIENT_MANDIR)/`echo $$f|sed 's,^,k,'`.1; \
 	done
-	$(INSTALL_DATA) $(srcdir)/tmac.doc ${DESTDIR}$(CLIENT_MANDIR)/tmac.doc
 
 authenc.o: defines.h externs.h general.h ring.h types.h $(ARPA_TELNET)
 commands.o: defines.h externs.h general.h ring.h types.h $(ARPA_TELNET)
