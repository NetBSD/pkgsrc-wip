$NetBSD: patch-ae,v 1.3 2007/09/05 12:04:31 bsadewitz Exp $

--- composite/compint.h.orig	2007-08-23 15:04:52.000000000 -0400
+++ composite/compint.h
@@ -232,6 +232,9 @@ compCheckTree (ScreenPtr pScreen);
 #define compCheckTree(s)
 #endif
 
+PictFormatPtr
+compWindowFormat (WindowPtr pWin);
+
 void
 compSetPixmap (WindowPtr pWin, PixmapPtr pPixmap);
 
