$NetBSD: patch-ai,v 1.1 2007/08/05 07:19:16 bsadewitz Exp $

--- hw/xfree86/loader/loadmod.c.orig	2007-06-30 11:16:04.000000000 -0400
+++ hw/xfree86/loader/loadmod.c
@@ -859,7 +859,7 @@ doLoadModule(const char *module, const c
     char *m = NULL;
     const char **cim;
 
-    xf86MsgVerb(X_INFO, 3, "LoadModule: \"%s\"", module);
+    xf86MsgVerb(X_INFO, 3, "LoadModule: \"%s\"\n", module);
 
     for (cim = compiled_in_modules; *cim; cim++)
 	if (!strcmp (module, *cim))
