$NetBSD: patch-cc,v 1.1 2007/08/18 19:25:34 bsadewitz Exp $

--- randr/rroutput.c.orig	2007-07-31 18:37:36.000000000 -0400
+++ randr/rroutput.c
@@ -249,6 +249,7 @@ RROutputDeleteUserMode (RROutputPtr outp
 
     memmove (output->userModes + m, output->userModes + m + 1,
 	     (output->numUserModes - m - 1) * sizeof (RRModePtr));
+    output->numUserModes--;
     RRModeDestroy (mode);
     return Success;
 }
