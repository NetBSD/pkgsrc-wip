--- db/nonce.cpp.orig	2011-02-13 21:56:16.000000000 -0500
+++ db/nonce.cpp	2011-02-13 21:56:03.000000000 -0500
@@ -32,7 +32,7 @@
 		if( _initialized ) return;
 		_initialized = true;
 
+#if defined(__linux__) || defined(__sunos__) || defined(__netbsd__)
-#if defined(__linux__) || defined(__sunos__)
         _devrandom = new ifstream("/dev/urandom", ios::binary|ios::in);
         massert( 10353 ,  "can't open dev/urandom", _devrandom->is_open() );
 #elif defined(_WIN32)
