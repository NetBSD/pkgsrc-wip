$NetBSD: patch-ab,v 1.2 2003/10/02 11:47:53 cubidou Exp $

--- src/view/gdk-view.c.orig	2003-09-29 09:21:00.000000000 +0200
+++ src/view/gdk-view.c
@@ -935,13 +935,13 @@ static void gdk_view_clear_score(GdkView
 
 void gdk_view_set_score(GdkView * d,int score) {
 	 Block * block;
+  int pre_score;
+  int i = 0;
+  
   g_assert(IS_GDK_VIEW(d));
   gdk_view_clear_score(d);
 
 
-  int pre_score;
-  int i = 0;
-  
 
   if( score != 0 ) {
 
