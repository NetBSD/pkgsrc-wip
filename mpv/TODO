- There is a regression at least when using the `sdl' ao and when
  playing multiple audio files they always get stuck at the end without
  automatically changing to the next file
- Check if oss and netbsd audio outputs can be updated/backported
- Double-check all options if still supported
- Check if newer Lua-s can be used
