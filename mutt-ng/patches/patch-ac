$NetBSD: patch-ac,v 1.1 2005/08/21 11:40:26 christian_gall Exp $

--- imap/auth_gss.c.orig	2005-07-17 13:45:15.000000000 +0200
+++ imap/auth_gss.c
@@ -68,6 +68,7 @@ imap_auth_res_t imap_auth_gss (IMAP_DATA
     return IMAP_AUTH_UNAVAIL;
   }
 #ifdef DEBUG
+#if 0
   else if (debuglevel >= 2) {
     maj_stat = gss_display_name (&min_stat, target_name, &request_buf,
                                  &mech_name);
@@ -76,6 +77,7 @@ imap_auth_res_t imap_auth_gss (IMAP_DATA
     maj_stat = gss_release_buffer (&min_stat, &request_buf);
   }
 #endif
+#endif
   /* Acquire initial credentials - without a TGT GSSAPI is UNAVAIL */
   sec_token = GSS_C_NO_BUFFER;
   context = GSS_C_NO_CONTEXT;
