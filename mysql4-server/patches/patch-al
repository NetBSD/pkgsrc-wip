$NetBSD: patch-al,v 1.4 2003/07/10 16:48:41 marc Exp $

--- include/my_pthread.h.orig	2003-05-14 22:24:34.000000000 +0200
+++ include/my_pthread.h	2003-07-10 18:39:44.000000000 +0200
@@ -19,7 +19,13 @@
 #ifndef _my_pthread_h
 #define _my_pthread_h
 
+/* defines __NetBSD_Version__ */
+#if defined(__NetBSD__)
+#include <sys/param.h>
+#endif
+
 #include <errno.h>
+#include <signal.h>
 #ifndef ETIME
 #define ETIME ETIMEDOUT				/* For FreeBSD */
 #endif
@@ -298,7 +304,11 @@
 #endif
 
 #ifndef HAVE_NONPOSIX_SIGWAIT
+#if defined(__NetBSD__) && (__NetBSD_Version__ >= 106130000)
 #define my_sigwait(A,B) sigwait((A),(B))
+#elif defined (__NetBSD__) && (__NetBSD_Version__ < 106130000)
+#define my_sigwait(A,B) __pthread_sigwait((A),(B))
+#endif
 #else
 int my_sigwait(const sigset_t *set,int *sig);
 #endif
