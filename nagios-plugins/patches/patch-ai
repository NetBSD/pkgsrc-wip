$NetBSD: patch-ai,v 1.2 2005/12/27 21:41:30 absd Exp $

--- plugins-scripts/subst.in.orig	2005-08-30 23:11:42.000000000 +0100
+++ plugins-scripts/subst.in
@@ -9,13 +9,14 @@ function which(c,path) {
 
 	sub(/\/.*\//,"",c);
   for (dir in path) {
+	        if (match(path[dir], /.tools/ ) != 0 ) {
+		    continue;
+		}
 			cmd = "test -x " path[dir] "/" c;
 			if (system(cmd)==0) {
 					return path[dir] "/" c;
 			} 
 	}
-
-
 	return c;
 }
 
