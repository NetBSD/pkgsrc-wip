$NetBSD: patch-ac,v 1.4 2009/08/18 15:26:29 plunky Exp $

--- bluetooth/hci.h.orig	2009-08-18 15:53:42.000000000 +0100
+++ bluetooth/hci.h
@@ -39,6 +39,7 @@ extern "C" {
 #define HCI_MAX_EVENT_SIZE	260
 #define HCI_MAX_FRAME_SIZE	(HCI_MAX_ACL_SIZE + 4)
 
+#if 0
 /* HCI dev events */
 #define HCI_DEV_REG	1
 #define HCI_DEV_UNREG	2
@@ -94,6 +95,7 @@ enum {
 #define HCISETSCOMTU	_IOW('H', 228, int)
 
 #define HCIINQUIRY	_IOR('H', 240, int)
+#endif
 
 #ifndef __NO_HCI_DEFS
 
@@ -1708,6 +1710,7 @@ typedef struct {
 
 #endif /* _NO_HCI_DEFS */
 
+#if 0
 /* HCI Socket options */
 #define HCI_DATA_DIR	1
 #define HCI_FILTER	2
@@ -1815,6 +1818,12 @@ struct hci_inquiry_req {
 	uint8_t  num_rsp;
 };
 #define IREQ_CACHE_FLUSH 0x0001
+#else
+#define HCI_CMSG_DIR	SCM_HCI_DIRECTION
+#define HCI_CMSG_TSTAMP	SCM_TIMESTAMP
+#define HCI_DEV_NONE	NULL
+#define HCI_DATA_DIR	SO_HCI_DIRECTION
+#endif
 
 #ifdef __cplusplus
 }
