$NetBSD: patch-image-mng,v 1.1.1.1 2008/01/26 04:42:52 hoka_adam Exp $

--- image/mng.c.orig	2007-08-12 00:07:51.000000000 +0200
+++ image/mng.c
@@ -356,7 +356,7 @@ mng_uint32 nsmng_gettickcount(mng_handle
 	static bool start = true;
 	static time_t t0;
 	struct timeval tv;
-#if defined(__SVR4) && defined(__sun)
+#if defined(__NetBSD__) || defined(__SVR4) && defined(__sun)
 	/* Solaris doesn't have this structure, and ignores the second
 	 * parameter to gettimeofday()
 	 */
