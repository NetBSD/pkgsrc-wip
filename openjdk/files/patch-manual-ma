$NetBSD: patch-manual-ma,v 1.2 2009/10/07 12:04:45 tnn2 Exp $

--- openjdk/jdk/src/share/classes/sun/applet/PluginAppletViewer.java.orig2	2009-10-06 16:21:54.000000000 +0200
+++ openjdk/jdk/src/share/classes/sun/applet/PluginAppletViewer.java
@@ -44,7 +44,7 @@ import sun.awt.SunToolkit;
 import sun.awt.AppContext;
 import sun.awt.X11.*;
 import java.lang.ref.WeakReference;
-import net.sourceforge.jnlp.NetxPanel;
+//import net.sourceforge.jnlp.NetxPanel;
 
 /**
  * Lets us construct one using unix-style one shot behaviors
@@ -105,6 +105,7 @@ public class PluginAppletViewer extends 
     	this.factory = factory;
 	this.statusMsgStream = statusMsgStream;
 
+/*
 	try {
 		panel = new NetxPanel(doc, atts);
 		AppletViewerPanel.debug("Using NetX panel");
@@ -112,6 +113,8 @@ public class PluginAppletViewer extends 
 		AppletViewerPanel.debug("Unable to start NetX applet - defaulting to Sun applet", ex);
 		panel = new AppletViewerPanel(doc, atts);
 	}
+*/
+	panel = new AppletViewerPanel(doc, atts);
 	add("Center", panel);
 	panel.init();
 	appletPanels.addElement(panel);
