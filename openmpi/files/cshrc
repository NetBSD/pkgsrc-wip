# Generated by openmpi package
echo $PATH | fgrep -q %%MPI_PREFIX%%
if ( $status != 0 ) then
    setenv MPI_PREFIX           %%MPI_PREFIX%%
    setenv PATH                 $MPI_PREFIX/bin:$PATH
    setenv MANPATH              $MPI_PREFIX/man:$MANPATH
endif

