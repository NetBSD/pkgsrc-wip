- After pgstudio.war opened, pgstudio directory created. But it
  is not controlled by pkg database
  (Is it to be installed on ${VARBASE} ?)
- currently apache-tomcat-8.0 is hardcoded as DEPENDS+=
   but any version (7 or 8 or 8.5), can be used.
