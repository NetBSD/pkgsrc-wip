$NetBSD: patch-ai,v 1.1 2007/09/28 05:12:00 bsadewitz Exp $

--- pixman/pixman-image.c.orig	2007-09-28 01:10:54.000000000 -0400
+++ pixman/pixman-image.c
@@ -357,8 +357,11 @@ pixman_image_create_bits (pixman_format_
     
     image = allocate_image();
 
-    if (!image)
+    if (!image) {
+	if (free_me)
+	    free (free_me);
 	return NULL;
+    }
     
     image->type = BITS;
     image->bits.format = format;
