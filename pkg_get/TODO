handle when dependency has multiple versions, for example:

sudo pkg_get install scmgit
... downloaded both tk-8.3.4nb3.tgz and tk-8.4.15.tgz
and also both versions of tcl.
wasted downloads
installs fine though

