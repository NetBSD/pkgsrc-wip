$NetBSD: patch-ad,v 1.1 2007/11/13 23:32:17 bsadewitz Exp $

--- src/modules/module-solaris.c.orig	2007-11-11 23:35:19.000000000 -0500
+++ src/modules/module-solaris.c
@@ -116,7 +116,11 @@ static const char* const valid_modargs[]
 
 #define DEFAULT_SINK_NAME "solaris_output"
 #define DEFAULT_SOURCE_NAME "solaris_input"
+#ifdef DEVSUNAUDIO
+#define DEFAULT_DEVICE DEVSUNAUDIO
+#else
 #define DEFAULT_DEVICE "/dev/audio"
+#endif
 
 static int sink_process_msg(pa_msgobject *o, int code, void *data, int64_t offset, pa_memchunk *chunk) {
     struct userdata *u = PA_SINK(o)->userdata;
