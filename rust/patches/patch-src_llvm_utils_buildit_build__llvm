$NetBSD: patch-src_llvm_utils_buildit_build__llvm,v 1.1 2014/01/11 18:29:56 schmonz Exp $

Shell portability fix.

--- src/llvm/utils/buildit/build_llvm.orig	2014-01-03 03:07:25.000000000 +0000
+++ src/llvm/utils/buildit/build_llvm
@@ -222,7 +222,7 @@ cd $DIR/obj-llvm || exit 1
 
 # Install the tree into the destination directory.
 make $JOBS_FLAG $COMMON_MAKEFLAGS UNIVERSAL_ARCH="$HOSTS" install
-if ! test $? == 0 ; then
+if ! test $? = 0 ; then
     echo "error: LLVM 'make install' failed!"
     exit 1
 fi 
