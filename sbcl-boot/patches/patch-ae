$NetBSD: patch-ae,v 1.1.1.1 2008/01/05 00:07:16 asau Exp $

Fix build with CLISP.

--- src/compiler/policy.lisp.orig	2007-09-10 03:07:25.000000000 +0400
+++ src/compiler/policy.lisp	2007-09-10 03:08:19.000000000 +0400
@@ -37,7 +37,7 @@
 bodies of code with eg. a known minimum safety.
 
 EXPERIMENTAL INTERFACE: Subject to change."
-  (declare (policy-quality min))
+  #-clisp(declare (policy-quality min))
   (when quality
     (aver (policy-quality-name-p quality))
     (if (zerop min)
