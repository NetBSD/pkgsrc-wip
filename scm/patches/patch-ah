$NetBSD: patch-ah,v 1.1.1.1 2003/04/13 11:02:10 jmmv Exp $

--- scm.c.orig	Sat Apr 13 13:35:53 2002
+++ scm.c
@@ -66,6 +66,9 @@
 # ifdef SVR4
 #  include <unistd.h>
 # endif
+# ifdef __NetBSD__
+#  include <unistd.h>
+# endif
 # ifdef __OpenBSD__
 #  include <unistd.h>
 # endif
@@ -705,6 +708,10 @@ void final_scm(freeall)
 #endif
 #ifdef __amigaos__
 # define SYSTNAME "amiga"
+# define DIRSEP "/"
+#endif
+#ifdef __NetBSD__
+# define SYSTNAME "unix"
 # define DIRSEP "/"
 #endif
 const char dirsep[] = DIRSEP;
