$NetBSD: patch-aj,v 1.1.1.1 2003/04/13 11:02:10 jmmv Exp $

--- socket.c.orig	Sat Apr 13 13:35:53 2002
+++ socket.c
@@ -69,6 +69,9 @@
 # ifdef SVR4
 #  include <unistd.h>
 # endif
+# ifdef __NetBSD__
+#  include <unistd.h>
+# endif
 # ifdef __OpenBSD__
 #  include <unistd.h>
 # endif
@@ -143,7 +146,7 @@ SCM l_hostinfo(name)
   struct in_addr inad;
   char **argv;
   int i = 0;
-#ifndef linux
+#if !(defined(linux) || defined(__NetBSD__))
   if UNBNDP(name) {
     DEFER_INTS;
     SYSCALL(entry = gethostent(););
