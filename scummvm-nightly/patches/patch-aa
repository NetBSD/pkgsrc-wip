$NetBSD: patch-aa,v 1.2 2008/02/19 17:28:33 thomasklausner Exp $

--- common/unzip.h.orig	2008-02-19 17:13:43.000000000 +0000
+++ common/unzip.h
@@ -79,6 +79,10 @@ extern "C" {
 #include <zlib.h>
 #endif
 
+#ifndef OF
+#define OF(X)	X
+#endif
+
 #if defined(STRICTUNZIP) || defined(STRICTZIPUNZIP)
 /* like the STRICT of WIN32, we define a pointer that cannot be converted
     from (void*) without cast */
