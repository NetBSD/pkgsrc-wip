$NetBSD: patch-ab,v 1.3 2009/01/15 16:04:44 thomasklausner Exp $

--- src/osd/sdl/sdlsync.c.orig	2008-12-12 04:55:30.000000000 +0100
+++ src/osd/sdl/sdlsync.c
@@ -586,7 +586,7 @@ int osd_thread_adjust_priority(osd_threa
 
 int osd_thread_cpu_affinity(osd_thread *thread, UINT32 mask)
 {
-#if !defined(NO_THREAD_COOPERATIVE) && !defined(NO_AFFINITY_NP)
+#if !defined(NO_THREAD_COOPERATIVE) && !defined(NO_AFFINITY_NP) && !defined(__NetBSD__)
 	cpu_set_t	cmask;
 	pthread_t	lthread;
 	int			bitnum;
