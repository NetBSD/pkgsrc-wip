$NetBSD: patch-am,v 1.1.1.1 2010/08/03 02:02:30 erinlalouve Exp $

--- src/plugins/websearch/query_context.cpp.orig	2010-06-12 13:26:07.000000000 +0000
+++ src/plugins/websearch/query_context.cpp
@@ -300,7 +300,7 @@ namespace seeks_plugins
 	       }
 	     
 	     // parse the output and create result search snippets.   
-	     int rank_offset = (i > 0) ? i * websearch::_wconfig->_N : 0;
+	     int rank_offset = (i > 0) ? i * websearch::_wconfig->N_ : 0;
 	     
 	     se_handler::parse_ses_output(outputs,nresults,_cached_snippets,rank_offset,this,se_enabled);
 	     for (int j=0;j<nresults;j++)
@@ -563,7 +563,7 @@ namespace seeks_plugins
 	else if (auto_lang == "ko")
 	  region_lang = "ko-KR";
 	else if (auto_lang == "ar")
-	  region_lang = "ar-EG"; // Egypt, with _NO_ reasons. In most cases, the search engines will decide based on the 'ar' code.
+	  region_lang = "ar-EG"; // Egypt, with N_O_ reasons. In most cases, the search engines will decide based on the 'ar' code.
 	else if (auto_lang == "be")
 	  region_lang = "be-BY";
 	else if (auto_lang == "bg")
