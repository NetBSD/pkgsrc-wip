- This package is really raw but it's a start, don't expect it to work
  out of the box

- sendmailx, sendailX, smx or smX - choose one and stick to it
- fix the current example smx.conf file (see TODO in the file)
- fix files/Makefile (createmap)

- Test TLS support
- Test SASL support
- Test on 1.6.x
- Test on 3.0_BETA
