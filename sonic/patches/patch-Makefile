$NetBSD$

Preserve PREFIX

--- Makefile.orig	2023-10-12 22:32:47.000000000 +0000
+++ Makefile
@@ -11,13 +11,13 @@
 # speech recognition.
 #USE_SPECTROGRAM=1
 
-PREFIX=/usr
-
 UNAME := $(shell uname)
 ifeq ($(UNAME), Darwin)
-  PREFIX=/usr/local
+  PREFIX?=/usr/local
 endif
 
+PREFIX?=/usr
+
 BINDIR=$(PREFIX)/bin
 LIBDIR=$(PREFIX)/lib
 INCDIR=$(PREFIX)/include
