# $NetBSD$

PKGNAME=	suse_glibc-locale-base-${SUSE_VERSION}
CATEGORIES=	emulators
RPMPKGS+=	glibc-locale-base-2.31-150300.41.1.${SUSE_ARCH}
COMMENT=	Linux compatibility package for glibc-locale-base

MAINTAINER=	pkgsrc-users@NetBSD.org
COMMENT=	Terminfo

.include "../../emulators/suse15_linux/Makefile.common"
.include "../../mk/bsd.pkg.mk"
