$NetBSD: patch-ad,v 1.1.1.1 2006/10/27 22:30:56 thomasklausner Exp $

--- ccutil/strngs.h.orig	2006-06-17 00:17:05.000000000 +0200
+++ ccutil/strngs.h
@@ -168,7 +168,7 @@ class DLLSYM STRING
       char *instring;            //input from read
       INT32 length;              //length of string
 
-      instring = (char *) de_serialise_bytes (f, (int) ptr);
+      instring = (char *) de_serialise_bytes (f, (long) ptr);
       length = (INT32) ptr;
       ptr = NULL;
       *this = instring;
