===========================================================================
$NetBSD$

Some functions of testssl.sh require setting the variable

  TESTSSL_INSTALL_DIR

to

  ${PREFIX}/etc/${PKGBASE}

===========================================================================
