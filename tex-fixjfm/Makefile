# $NetBSD$

DISTNAME=	fixjfm
PKGNAME=	tex-${DISTNAME}-0.8
TEXLIVE_REV=	47113

MAINTAINER=	pkgsrc-users@NetBSD.org
HOMEPAGE=	https://ctan.org/pkg/fixjfm
COMMENT=	Fix JFM (for *pTeX)
# LICENSE=	knuth

.include "../../print/texlive/package.mk"
.include "../../mk/bsd.pkg.mk"
