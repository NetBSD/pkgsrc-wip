$NetBSD: patch-aa,v 1.4 2006/11/10 19:07:00 plunky Exp $

--- src/unshield.c.orig	2005-07-08 15:08:37.000000000 +0100
+++ src/unshield.c
@@ -2,3 +2,3 @@
 #define _BSD_SOURCE 1
-#define _POSIX_C_SOURCE 2
+/*#define _POSIX_C_SOURCE 2*/
 #include "../lib/libunshield.h"
@@ -280,6 +280,6 @@ static bool extract_file(Unshield* unshi
       default:
-        if (!isprint(*p))
+        if (!isprint((int)*p))
           *p = '_';
         else if (make_lowercase)
-          *p = tolower(*p);
+          *p = tolower((int)*p);
         break;;
@@ -300,6 +300,6 @@ static bool extract_file(Unshield* unshi
   {
-    if (!isprint(*p))
+    if (!isprint((int)*p))
       *p = '_';
     else if (make_lowercase)
-      *p = tolower(*p);
+      *p = tolower((int)*p);
   }
