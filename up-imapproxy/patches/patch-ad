$NetBSD: patch-ad,v 1.1 2006/05/05 22:13:18 adrian_p Exp $

--- src/main.c.orig	2005-08-31 17:32:24.000000000 +0100
+++ src/main.c
@@ -615,7 +615,7 @@ int main( int argc, char *argv[] )
 	exit( 1 );
     }
 
-    syslog( LOG_INFO, "%s: Normal server startup.", fn );
+    syslog( LOG_INFO, "%s: imapproxy version %s normal server startup.", fn, IMAP_PROXY_VERSION );
 
     /*
      * Main server loop
@@ -849,7 +849,7 @@ static int ParseBannerAndCapability( cha
 	exit( 1 );
     }
     
-    sprintf( DestBuf, CP );
+    sprintf( DestBuf, "%s", CP );
     
     /*
      * initially assume that the server doesn't support UNSELECT.
