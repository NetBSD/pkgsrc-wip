$NetBSD: patch-ab,v 1.1.1.1 2003/09/24 06:21:43 bencollver Exp $

--- Makefile.inc.orig	Mon Mar 12 06:30:32 2001
+++ Makefile.inc
@@ -4,6 +4,9 @@ ifneq ($(strip $(wildcard $(UDIR)/includ
 DEFS += -DWITH_$(U)
 INCLUDES += -I$(UDIR)/include
 
+ifneq ($(strip $(wildcard $(UDIR)/lib)),)
+LIBDIRS += $(UDIR)/lib
+endif
 ifneq ($(strip $(wildcard $(UDIR)/build/$(target)/src/.libs)),)
 LIBDIRS += $(UDIR)/build/$(target)/src/.libs
 endif
