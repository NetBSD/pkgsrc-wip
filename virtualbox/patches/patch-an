$NetBSD: patch-an,v 1.1 2009/08/12 12:54:17 tnn2 Exp $

--- src/VBox/Runtime/r3/posix/semevent-posix.cpp.orig	2009-08-04 19:18:58.000000000 +0200
+++ src/VBox/Runtime/r3/posix/semevent-posix.cpp
@@ -46,7 +46,7 @@
 # define pthread_yield() pthread_yield_np()
 #endif
 
-#ifdef RT_OS_SOLARIS
+#if defined(RT_OS_SOLARIS) || defined(RT_OS_NETBSD)
 # include <sched.h>
 # define pthread_yield() sched_yield()
 #endif
@@ -364,7 +364,7 @@ static int  rtSemEventWait(RTSEMEVENT Ev
             pthread_yield();
 
         /* take mutex */
-#ifdef RT_OS_DARWIN
+#if defined(RT_OS_DARWIN) || defined(RT_OS_NETBSD)
         int rc = pthread_mutex_lock(&pIntEventSem->Mutex);
 #else
         int rc = pthread_mutex_timedlock(&pIntEventSem->Mutex, &ts);
