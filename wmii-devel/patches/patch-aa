$NetBSD: patch-aa,v 1.3 2007/05/15 23:14:34 netcap Exp $

--- config.mk.orig	2007-03-17 22:52:07.000000000 +0100
+++ config.mk
@@ -5,22 +5,21 @@ CONFVERSION = 3.5
 # Customize below to fit your system
 
 # paths
-PREFIX = /usr/local
-CONFPREFIX = ${PREFIX}/etc
-MANPREFIX = ${PREFIX}/share/man
+CONFPREFIX = ${PREFIX}/share/examples
+MANPREFIX = ${PREFIX}/${PKGMANDIR}
 
-X11INC = /usr/X11R6/include
-X11LIB = /usr/X11R6/lib
+X11INC = ${X11BASE}/include
+X11LIB = ${X11BASE}/lib
 
 # includes and libs
 INCS = -I. -I${PREFIX}/include -I/usr/include -I${X11INC}
-LIBS = -L/usr/lib -lc -L${X11LIB} -lX11 -L${PREFIX}/lib -lixp
+LIBS = --lc -lX11 -lixp
 
 # flags
 #CFLAGS = -Os ${INCS} -DVERSION=\"${VERSION}\"
 #LDFLAGS = ${LIBS}
-CFLAGS = -g -Wall ${INCS} -DVERSION=\"${VERSION}\"
-LDFLAGS = -g ${LIBS}
+CFLAGS = -DVERSION=\"${VERSION}\"
+LDFLAGS += ${LIBS}
 
 # Solaris
 #CFLAGS = -fast ${INCS} -DVERSION=\"${VERSION}\"
