$NetBSD: patch-al,v 1.1.1.1 2011/01/02 05:42:34 rubengonzalez Exp $

--- cmd_getwindowname.c.orig	2010-08-17 15:22:11.000000000 +0000
+++ cmd_getwindowname.c
@@ -13,7 +13,7 @@ int cmd_getwindowname(context_t *context
     HELP_SEE_WINDOW_STACK;
   int option_index;
 
-  while ((c = getopt_long_only(context->argc, context->argv, "+h",
+  while ((c = getopt_long(context->argc, context->argv, "+h",
                                longopts, &option_index)) != -1) {
     switch (c) {
       case 'h':
