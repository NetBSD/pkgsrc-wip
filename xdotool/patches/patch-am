$NetBSD: patch-am,v 1.1.1.1 2011/01/02 05:42:34 rubengonzalez Exp $

--- cmd_getwindowpid.c.orig	2010-06-13 09:59:59.000000000 +0000
+++ cmd_getwindowpid.c
@@ -14,7 +14,7 @@ int cmd_getwindowpid(context_t *context)
     HELP_SEE_WINDOW_STACK;
   int option_index;
 
-  while ((c = getopt_long_only(context->argc, context->argv, "+h",
+  while ((c = getopt_long(context->argc, context->argv, "+h",
                                longopts, &option_index)) != -1) {
     switch (c) {
       case 'h':
