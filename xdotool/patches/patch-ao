$NetBSD: patch-ao,v 1.1.1.1 2011/01/02 05:42:34 rubengonzalez Exp $

--- cmd_mousedown.c.orig	2010-06-13 09:33:16.000000000 +0000
+++ cmd_mousedown.c
@@ -22,7 +22,7 @@ int cmd_mousedown(context_t *context) {
             "--clearmodifiers       - reset active modifiers (alt, etc) while typing\n";
   int option_index;
 
-  while ((c = getopt_long_only(context->argc, context->argv, "+chw:",
+  while ((c = getopt_long(context->argc, context->argv, "+chw:",
                                longopts, &option_index)) != -1) {
     switch (c) {
       case 'c':
