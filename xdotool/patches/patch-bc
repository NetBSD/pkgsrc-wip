$NetBSD: patch-bc,v 1.1.1.1 2011/01/02 05:42:34 rubengonzalez Exp $

--- cmd_windowkill.c.orig	2010-08-10 00:37:29.000000000 +0000
+++ cmd_windowkill.c
@@ -15,7 +15,7 @@ int cmd_windowkill(context_t *context) {
     HELP_SEE_WINDOW_STACK;
   int option_index;
 
-  while ((c = getopt_long_only(context->argc, context->argv, "+h",
+  while ((c = getopt_long(context->argc, context->argv, "+h",
                                longopts, &option_index)) != -1) {
     switch (c) {
       case 'h':
