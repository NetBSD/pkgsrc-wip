$NetBSD: patch-bf,v 1.1.1.1 2011/01/02 05:42:34 rubengonzalez Exp $

--- cmd_windowmove.c.orig	2010-07-10 02:36:00.000000000 +0000
+++ cmd_windowmove.c
@@ -33,7 +33,7 @@ int cmd_windowmove(context_t *context) {
     "--sync    - only exit once the window has moved\n";
 
   int option_index;
-  while ((c = getopt_long_only(context->argc, context->argv, "+h",
+  while ((c = getopt_long(context->argc, context->argv, "+h",
                                longopts, &option_index)) != -1) {
     switch (c) {
       case 'h':
