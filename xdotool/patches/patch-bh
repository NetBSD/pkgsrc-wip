$NetBSD: patch-bh,v 1.1.1.1 2011/01/02 05:42:34 rubengonzalez Exp $

--- cmd_windowreparent.c.orig	2010-08-10 00:37:29.000000000 +0000
+++ cmd_windowreparent.c
@@ -16,7 +16,7 @@ int cmd_windowreparent(context_t *contex
   static const char *usage = "Usage: %s [window_source=%1] window_destination\n";
 
   int option_index;
-  while ((c = getopt_long_only(context->argc, context->argv, "+h",
+  while ((c = getopt_long(context->argc, context->argv, "+h",
                                longopts, &option_index)) != -1) {
     switch (c) {
       case 'h':
