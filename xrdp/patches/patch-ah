$NetBSD: patch-ah,v 1.3 2007/02/27 13:15:22 lkundrak Exp $

--- xup/Makefile.orig	2006-03-04 02:02:09.000000000 +0100
+++ xup/Makefile
@@ -1,20 +1,13 @@
 # libxup makefile
 XUPOBJ = os_calls.o xup.o
 
-DESTDIR = /usr/local/xrdp
-CFGDIR = /etc/xrdp
-PIDDIR = /var/run
-MANDIR = /usr/local/man
-DOCDIR = /usr/doc/xrdp
-
 DEFINES =
 
-CFLAGS = -Wall -O2 -I../common -fPIC $(DEFINES)
+CFLAGS += -Wall -I../common -fPIC $(DEFINES)
 #CFLAGS += -DXRDP_DEBUG
 C_OS_FLAGS = $(CFLAGS) -c
 LDFLAGS = -shared
-LIBS = -ldl
-CC = gcc
+LIBS = $(DLLIB)
 
 all: xup
 
@@ -25,10 +18,10 @@ clean:
 	rm -f $(XUPOBJ) libxup.so
 
 os_calls.o: ../common/os_calls.c
-	$(CC) $(C_OS_FLAGS) ../common/os_calls.c
+	$(CC) $(CPPFLAGS) $(C_OS_FLAGS) ../common/os_calls.c
 
 install:
-	install libxup.so $(DESTDIR)/libxup.so
+	$(BSD_INSTALL_LIB) libxup.so $(DESTDIR)/lib/libxup.so
 
 installdeb:
 	install libxup.so $(DESTDIRDEB)/usr/xrdp/libxup.so
