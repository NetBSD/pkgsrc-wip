$NetBSD$

make it easier to tell make where to start

--- makefile.orig	2014-03-21 08:55:27.000000000 +0000
+++ makefile
@@ -10,6 +10,8 @@ default  := $(out_base)/
 
 # Build.
 #
+all: $(default)
+
 $(default): $(out_base)/xsd/xsd/
 
 # Test.
