this installs some of the libexec commands to sbin ... is this okay?

MAKE_DIRS for /var/yp/securenets ??

This is an YP (NIS version 2) Server for Linux also runs under
other OS like SunOS 4.1.x, Solaris 2.x, AIX, HP-UX, IRIS, Ultrix
and OSF1 (alpha).
* needs to test other platforms
doesn't build under NetBSD.

The README says it uses gdbm or ndbm, but to
"NEVER use libdb1-libdb4 in ndbm compat mode, this does NOT work!"

Do not install man pages to share/man, but man.
