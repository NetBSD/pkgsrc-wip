$NetBSD: patch-ac,v 1.1.1.1 2008/09/09 02:32:19 bsadewitz Exp $

--- Src/zsh.h.orig	2008-09-08 18:17:50.000000000 -0400
+++ Src/zsh.h
@@ -1968,6 +1968,7 @@ struct ttyinfo {
 #endif
 };
 
+#ifndef __INTERIX
 /* defines for whether tabs expand to spaces */
 #if defined(HAVE_TERMIOS_H) || defined(HAVE_TERMIO_H)
 #define SGTTYFLAG       shttyinfo.tio.c_oflag
@@ -1985,6 +1986,7 @@ struct ttyinfo {
 #   endif
 #  endif
 # endif
+#endif
 
 /* flags for termflags */
 
